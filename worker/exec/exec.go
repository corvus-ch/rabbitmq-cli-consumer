// Package exec holds a worker implementation which runs a worker script as an
// external command.
package exec

import "github.com/corvus-ch/rabbitmq-cli-consumer/worker"

// TODO Refactor existing code into this package.

func New() (worker.Process, error) {
	// TODO implement.
	// Add parameters for logger, config etc. as needed.
	return nil, nil
}
